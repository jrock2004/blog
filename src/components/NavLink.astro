---
export interface Props {
  href: string;
  isActive: boolean;
}

const { href, isActive } = Astro.props;
---

<a class={`p-3 ${isActive ? 'font-semibold' : 'font-normal'} hover:bg-gray-100`} href={href}
  ><slot /></a
>
